# CSS Foundations v1 — Task 13 最终课程审核报告

审核日期：2026-09-23

审核分支：`feat/css-foundations-v1-curriculum-plan`

审核基线：`10532be5d38e7083055673ea633b2e88581f2f83`

## 1. Course snapshot

- 9 个 Module，metadata order 为 1–9。
- 32 个 Lesson，Lesson order 与冻结的课程规划一致。
- 100 个 Exercise；每个 Exercise 都有 `exercise.json`、fixture、base、starter、solution 五类 source asset。
- Lesson status：32 published。
- Exercise status：100 published；既有 Flexbox Exercise 的 stable IDs 保留。
- 当前生成 registry 已覆盖全部 32 个 Lesson。
- repository-wide stable id、同 parent 内 slug/order、Exercise 引用顺序均通过确定性审计。

## 2. Dependency and progression

```text
M1 css-language-and-selection
  → M2 cascade-and-values
  → M3 box-model-and-flow
      → M4 visual-styling-and-typography
      → M5 flexbox
      → M6 grid
          M3 + M4 → M7 flow-positioning-and-layering
          M3 + M5 + M6 → M8 responsive-css
M1–M8 → M9 integration-and-debugging
```

M5 的最终顺序为：

```text
flex-formatting-context-and-axes
→ flexbox-alignment
→ flex-wrapping-and-multiline-alignment
→ flexible-items-and-free-space
→ flex-item-control-and-order
```

既有 `flexbox-alignment` Lesson、stable ID、slug、status 和既有 Exercise stable IDs 均保留；新增了 row/column transfer，alignment Lesson 不再承担第一课的 Flex 基础教学。

## 3. Source grounding review

课程语义按 evidence hierarchy 复核：

- CSS Selectors Level 4、CSS Syntax。
- CSS Cascading and Inheritance、CSS Values and Units、CSS Custom Properties。
- CSS Box Model、CSS Sizing、CSS Overflow、CSS2 normal flow 与 margin collapsing。
- CSS Backgrounds and Borders、CSS Fonts、CSS UI/Selectors。
- CSS Flexible Box Layout、CSS Box Alignment。
- CSS Grid Layout、CSS Positioned Layout、Media Queries。
- MDN CSS debugging、containing block、sticky、stacking context、float 和 responsive guidance，用于 learner-facing explanation 与浏览器行为交叉核对。

课程没有把 freeCodeCamp 当作 CSS semantics authority；其作用仅限于和冻结的 coverage、progression、practice cadence 进行对照。没有发现需要为了 benchmark 增加 v1 scope 外 Module 的理由。

## 4. Final implementation-fit distribution

```text
READY                          24
TEACHABLE_BUT_CHECKER_LIMITED   8
NEEDS_CHECKER_CAPABILITY        0
NEEDS_ACTIVITY_CAPABILITY       0
NEEDS_WORKSPACE_CAPABILITY      0
```

`READY` 覆盖全部已发布的 CSS Foundations v1 learner chain；`TEACHABLE_BUT_CHECKER_LIMITED` 仅保留给最终结果仍依赖 Preview review 的 geometry、scroll、paint/occlusion、连续 viewport 与等价实现判断。所有 32 个 Lesson 均已进入 learner chain，但 checker 不把这些边界伪装成完整自动化证明。

## 5. Capability Gap Register

| Gap | Category | Affected Lessons | Severity | Blocks publication? | Suggested future milestone |
| --- | --- | --- | --- | --- | --- |
| source-aware CSS validation | Checker | M1 selector lessons；M2 cascade/value strategy；M8 media query；M9 diagnosis/capstone | Resolved | No | `rule-style` 读取 learner stylesheet 的 CSSOM 规则、selector、property、source order 与可选 media context；仍不判断等价 selector 意图 |
| geometry/layout validation | Checker | M3 intrinsic/spacing；M5 wrapping/free-space；M6 implicit/alignment；M7 positioning/float；M8/M9 integrated outcomes | High | Blocks lessons whose endpoint is final geometry；不阻塞明确 declaration subset | 增加等价解容忍的 layout observation/check protocol |
| pseudo-state validation | Checker | M4 `:hover`、`:focus`、`:focus-visible` | Partially resolved | No for authored declarations；Yes for real input modality | `rule-style` 已覆盖 pseudo-class authored declarations；真实 pointer/keyboard modality、paint/occlusion 仍需 Preview |
| scroll-state validation | Checker | M7 fixed/sticky positioning | High | Yes，对 sticky threshold/scrollport 的完整 mastery | 增加 deterministic scroll container 与 scroll assertions |
| paint/stacking validation | Checker | M7 stacking context and z-index | High | Yes，对 occlusion/paint order 的完整 mastery | 增加 paint/occlusion observation API |
| multi-viewport validation | Checker | M8 三课；M9 responsive capstone | Resolved for fixed matrix | No for 390/768/1280 checks；Yes for arbitrary ranges and full geometry | `viewport-style` 为每个 unique width 创建隔离 probe iframe，当前支持 390/768/1280；连续范围与完整 geometry 仍属 Preview review |
| authored Grid track validation | Checker | M6 grid-formatting-context-and-tracks；M8 Grid responsive exercise | Resolved for declared subset | No for explicit checked declarations；Yes for full layout equivalence | `rule-style` 可检查 authored `grid-template-columns`；不宣称 track sizing/geometry 等价判断 |

最终课程没有把 pseudo-elements 作为 learning outcome，因此没有机械保留 pseudo-element gap。当前 fixed HTML + CSS-only Learner Contract 不需要 HTML Editor、JS Editor、新 Activity 或新的 Workspace capability。

## 6. Checker and publication review

- `style`、`rule-style`、`viewport-style`、`exists` 与 `count` 均使用声明式 content data；没有 custom per-exercise JavaScript checker，也没有把固定像素几何冒充 mental-model mastery。
- `rule-style` 只证明 authored selector/property/value 及可选 media context；`viewport-style` 只证明指定固定视口下的 computed outcome。pointer/keyboard modality、scroll threshold、paint/occlusion、任意连续宽度与所有等价实现仍明确交给 Preview review。
- 100 个 Exercise 的 checks 均非空，0 个 published learner entry 缺少检查；32 个 Lesson 的引用顺序、提示与 content contract 均通过。
- M4 interaction、M6 tracks、M8 responsive 与 M9 capstone 已发布可验证 declaration subset；其未覆盖的行为边界保留在课程文案中，没有下调为虚假通过。
- Workspace fit：fixed HTML + CSS-only 边界与当前 Learner Contract 一致；未扩大为 HTML editor、JS editor 或 generic IDE。

## 7. Three review perspectives

### R1 — CSS Technical

逐课检查了 selector/cascade/value、box/flow、Flex axes/free space、Grid track/line、containing block、sticky preconditions、stacking context、float、media condition 与 debugging pipeline。未发现需要立即修复的 Critical/Major factual defect；受限行为均有边界说明。

### R2 — Curriculum

检查了 M1→M9 dependency、相邻 Lesson non-redundancy、row/column transfer、Grid line/track distinction、Flex item/line distinction、source-order caution、responsive progression 和 M9 synthesis。未发现需要拆分/重排才能继续的 Critical/Major defect；capability-limited outcomes 已通过声明检查与 Preview review 的边界说明纳入 published learner chain。

### R3 — Product / Checker

检查了 Activity v1 fit、固定 fixture、Exercise asset completeness、checker false-positive/false-negative 风险、Preview 390/768/1280 的固定矩阵与 generated registry。未发现需要新增平台 capability 才能发布当前 learner chain 的课程内容 defect；真实未覆盖边界已登记。三次 delegated reviewer 尝试未在环境时限内返回，因此本报告不把它们伪报为通过；上述 R1/R2/R3 结论来自主进程的独立只读复核。

## 8. Validation record

已实际运行并通过：

```text
pnpm content:generate
pnpm content:check        # 32 lesson(s)
pnpm test:content         # 22/22
pnpm test:authoring-skill # 11/11
pnpm lint
pnpm build
pnpm test:e2e
git diff --check
```

`pnpm test:e2e` 覆盖 Studio 100 条 learner links、hydration gate、编辑器/Preview/check/persistence、source-aware rule checks、pseudo-class checks 与固定 viewport matrix。Generator idempotency 通过再次运行 `pnpm content:generate`，并以 `git diff --exit-code -- src/features/learning/generated/lesson-content-registry.tsx` 验证。

## 9. Main branch merge-readiness

已无代理 fetch 最新 `origin/main`：`9762a9ed06aeeec296591f06d43ea517b79e77ad`。它相对当前 feature branch 引入 M6A v6 Exercise v2 / Workspace / Browser Runtime contract，包括：

- Exercise `schemaVersion: 2`、workspace file list 与 browser runtime entry。
- `starter/`、`solution/` 新 asset layout。
- reader、schema、checker/runtime 和 authoring skill contract 的同步变化。

当前 feature branch 仍遵循本任务冻结的 v1 Exercise contract。没有擅自 merge、rebase 或迁移 v1 curriculum；正式合并前需要单独决定 M6A contract migration/rebase 策略并重新跑完整 validation。

因此本分支截至本报告是 content-audited、E2E-verified、report-ready。它不包含对 `main` 的 merge/rebase，也不宣称已完成 M6A contract migration；该独立同步策略仍需后续单独决定。
