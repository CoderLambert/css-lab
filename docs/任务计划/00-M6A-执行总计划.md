# M6A — Workspace Domain / Front-end Lab Core 执行总计划

> Revision: v3  
> 本文档是 Codex 执行入口。执行前必须读取最新 `main`；历史 SHA 只说明设计时基线，不得替代实际代码审计。

## 1. 执行基线与原则

首次任务计划建立时：

```text
code baseline before planning docs:
2f617c50aab2f31943d5745760f5ed354d026a92

planning docs commit:
c4c22e3a824e068bfe70d6f3d16cac612280f683

current implementation baseline after MDX Learning Flow v1 merge:
d6f25eaffebeb4be771b0d8d2e2c5b6744573381
```

开始任何阶段前必须：

1. 切到 `main` 并拉取最新代码。
2. 阅读根目录 `AGENTS.md`。
3. 阅读当前阶段文档以及列出的相关代码。
4. 全局搜索当前阶段要替换的旧 API。
5. 如果 `main` 已包含部分目标改动，只补缺失项，不回滚新实现。
6. 每阶段结束时必须保持仓库可编译、可运行，不允许留下“下一阶段再修”的破坏性中间态。

### MDX Learning Flow v1 已是正式产品基线

`d6f25eaf` 之后，以下能力视为 M6A 必须保护的既有行为：

- `lesson.json` = Lesson runtime metadata；`lesson.mdx` = teaching content。
- Lesson runtime object 不携带 MDX source/body/path。
- generated lesson registry + `content:generate` / `content:check` / `test:content` 已进入正式 authoring/CI contract。
- MDX 受控 DSL 只允许 `Concept / Predict / Compare / Exercise`。
- `exercise.order` 是 learner navigation canonical sequence；learner-visible Lesson 的 MDX Exercise references 必须完整、唯一且同序。
- progressive hints、Preview bounded canvas + `Auto / 390 / 768 / 1280` presets、structured checker diagnostics 已是产品行为。
- style check 当前支持多个语义等价 computed values；M6A 必须保留该行为能力，但不在本 milestone 设计最终 matcher DSL。

Lesson Content Domain 与 Exercise Workspace Domain 必须保持分离：`lesson.mdx` 永远不是 Workspace file。

M6A 把当前：

```text
Exercise = fixtureHtml + baseCss + starterCss + learner css string
```

演进为：

```text
Disk Content
→ Workspace Definition
→ Learner Draft
→ Execution Snapshot
→ optional Toolchain
→ Runtime
→ Checker
→ Progress / Learning Shell
```

M6A 只实现 HTML/CSS Workspace + Browser Runtime，不实现 JavaScript/TypeScript 执行。

## 2. 已确定的架构结论

### Content / Schema

- Course / Module / Lesson 继续 `schemaVersion: 1`。
- Exercise 升级为 `schemaVersion: 2`。
- Task 01 只建立 v2 domain/schema definitions，不切生产 ContentReader。
- Task 02 才进行 Exercise v2 + content assets + ContentReader 的原子 cutover。
- `solution/` 永远不进入 learner-facing `Exercise`。
- ContentReader 负责 read + validate + hydrate，不负责 learner published visibility policy。

### Workspace

Workspace language vocabulary：

```text
html
css
javascript
typescript
```

这表示领域词汇，不表示当前 Runtime 已支持所有语言。

Workspace file：

```ts
{
  path: string;
  language: WorkspaceLanguage;
  editable: boolean;
}
```

规则：

- path 是 POSIX 风格 logical path，不是 OS filesystem path。
- file set 由 content author 声明。
- 允许受控子目录。
- learner 当前不能 create/delete/rename。
- locked file 使用 `editable: false`。
- locked file 不是 secret，不能包含答案。
- Workspace structural schema 允许零 editable file；是否适合作为 Exercise 由 Content Health 判断。
- Browser entry 属于 Runtime Definition，不属于 Workspace Definition。

### Starter / Solution

当前：

```text
fixture.html
base.css
starter.css
solution.css
```

迁移为：

```text
starter/
  index.html
  base.css
  style.css

solution/
  style.css
```

不创建 `support/`。`base.css` 是 locked Workspace file，因为它是真实 execution input。

`solution/` 只包含 editable files 的参考答案，path set 必须与 editable path set 完全相等。

### Learner Draft

Learner Draft：

- 只包含 editable files。
- 不包含 locked files。
- 不包含 solution。
- 不包含 runtime/compiler/console state。
- 不包含 active tab。
- dirty 从 starter + draft 派生，不持久化。

### Execution Snapshot

- 是 Runtime/Checker 的完整不可变输入。
- locked file 使用 starter content。
- editable file 使用 learner draft content。
- 保留 Workspace declaration order。
- Runtime 不知道 editable、Progress、solution 或 OS filesystem path。

### Runtime / Toolchain

- Browser / Worker 是 Runtime。
- TypeScript compiler 是 Toolchain。
- M6A 不添加 `toolchain: null`。
- 不创建 Worker Runtime placeholder。
- 不创建 RuntimeRegistry / ToolchainRegistry。

当前 Browser capability：

```text
HTML + CSS only
```

Workspace schema 可以表达 JavaScript/TypeScript，但：

- Studio Content Health 对当前 Browser exercise 中 JS/TS 报 error。
- 最终 Browser Runtime 对 JS/TS fail closed，绝不执行。
- Task 05 完成前，不向 content 添加 JS/TS workspace files。

### Checker

- style / exists / count 是 Browser DOM checker definitions。
- Check definition 可以 runtime/domain-specific。
- CheckResult 的公共语义保持 runtime-neutral，但必须继续区分 learner mismatch 与 checker/runtime fault，并保留足够 learner diagnostics。
- Browser-specific diagnostics（例如 selector/property）可以通过 Browser result/detail 承载；不要为了 neutralization 删除现有诊断能力。
- 当前 `StyleCheck.alsoAccepts` 是 transitional schema，但“一个 check 可接受多个语义等价 computed values”的行为必须保留。
- 不创建 CheckerRegistry，也不在 M6A 引入最终 matcher framework。

## 3. 阶段顺序与事务边界

必须按顺序：

1. Task 01 — Workspace Domain + Exercise v2 definitions
2. Task 02 — Content Assets + Exercise v2 atomic cutover + Studio
3. Task 03 — Progress v2 + IndexedDB migration
4. Task 04 — Workspace Editor + HTML/CSS editor capability
5. Task 05 — Execution Snapshot + Browser Runtime + security
6. Task 06 — Integration + regression + AGENTS/docs

Task 01 完成后：

- 旧 Exercise v1 production flow 仍完整工作。
- 新 Workspace/v2 definitions 已存在。
- 不允许出现“代码只认 v2，但磁盘仍 v1”。

Task 02 完成后：

- content、Exercise runtime type、ContentReader 一次性切 v2。
- 旧 `fixtureHtml/baseCss/starterCss` runtime contract 删除。

每阶段建议独立 commit。

## 4. 自动化测试策略

M6A 不为测试新增永久产品 route，也不为测试修改当前 3 个 CSS Exercise 的教学语义。

当前 3 个 Exercise 可以继续：

```text
index.html editable=false
base.css   editable=false
style.css  editable=true
```

验证分三层：

### A. 现有 learner E2E

继续覆盖真实 published CSS flow，并保留 MDX Learning Flow v1 已有回归意图：

- Lesson metadata H1 + MDX H2/H3 / Concept / Predict / Compare / Exercise Activity
- canonical Exercise navigation / Activity sibling links
- progressive hints
- Preview responsive/preset UX
- structured checker diagnostics 与语义等价 accepted value
- navigation
- CSS edit
- preview
- checker
- completion
- reload restore
- formatting / undo / swatches
- progress

### B. Workspace domain tests

使用现有 Playwright test runner 在 Node side 直接 import 纯函数，测试：

- multi-file Draft
- locked file rejection
- reset
- Execution Snapshot
- progress reconciliation

不引入第二套 test framework。

### C. Browser Runtime isolated iframe tests

Playwright 直接 import 纯 Browser document builder，在测试 page 中挂 sandboxed iframe，验证：

- HTML fragment mount
- CSS path slots
- runtime protocol
- script/event/javascript URL 防护
- invalid messages ignored

不要新增 `/__e2e__` 产品页面。

### Learner-route HTML tab E2E

首个真实 `editable: true` HTML Exercise 进入课程时，再补完整 learner-route HTML tab/persistence E2E。

M6A 必须实现该能力，但不伪造 published content 只为测试。

## 5. 全局非目标

禁止顺手实现：

- learner JavaScript execution
- TypeScript compiler
- Worker Runtime
- interaction checker
- Console
- npm / Vite runtime / WebContainer / terminal
- arbitrary packages
- React/Vue/Svelte Lab
- generic filesystem / file create/delete/rename
- cloud sync / auth
- global state library
- language/runtime/toolchain/checker plugin registry
- generic execution graph/event bus

## 6. 每阶段统一验证

至少：

```bash
pnpm content:check
pnpm test:content
pnpm lint
pnpm build
git diff --check
git status --short
```

M6A 会修改 Exercise content/schema、Studio 或 learner runtime，阶段完成时原则上同时运行：

```bash
pnpm test:e2e
```

涉及 Lesson 路径、MDX source、generated registry 或 content tooling 时，还必须：

```bash
pnpm content:generate
git diff --exit-code -- src/features/learning/generated/lesson-content-registry.tsx
```

M6A 正常情况下不应改变 Lesson registry；如果该命令产生 diff，应先解释原因，而不是直接把无关 registry 变更混入任务。

规则：

- 不 skip/delete 现有测试。
- 结构变化可以更新断言，但保留原测试意图。
- 不改变 `next build --webpack` 规避问题。
- 不自动删除 IndexedDB 作为 migration fallback。

## 7. 最终 Definition of Done

- MDX Learning Flow v1 行为无回归：受控 DSL、generated registry、progressive hints、Preview presets、structured diagnostics 与 canonical Exercise sequence 全部保留。
- `pnpm content:check` 与 `pnpm test:content` 在最终状态通过。
- 当前 3 个 CSS Exercise 行为无回归。
- Exercise runtime domain 不再有 `fixtureHtml/baseCss/starterCss`。
- Progress runtime contract 不再有 `code: string`。
- Workspace Editor 支持 content-defined editable HTML/CSS files。
- 当前 CSS Exercise 可以继续保持 HTML locked。
- locked HTML/CSS 参与 execution，但不进入 Learner Draft。
- solution 永不进入 learner props/runtime domain。
- Browser Runtime 只消费 Runtime Definition + Execution Snapshot + check request。
- CSS edit 无 iframe reload hot update。
- HTML content change 通过 document rebuild 更新 Runtime。
- learner HTML 不获得任意 JavaScript execution。
- v1 IndexedDB progress 可迁移到 v2。
- completed exercise 后续编辑仍保持 completed achievement。
- Studio 能检查 Workspace authoring semantics 和 solution completeness。
- AGENTS.md 更新为 Front-end Lab Platform 长期方向。
- 不提前实现 M6B / JavaScript Runtime / TypeScript Toolchain。
